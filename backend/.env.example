# Sao chép thành backend/.env (file .env không commit).

# Chuỗi kết nối Prisma — khớp docker-compose (user/pass/db mặc định).
DATABASE_URL="postgresql://eaai:eaai_dev_change_me@localhost:5432/eaai_com?schema=public"

# API
PORT=3000

# JWT — bắt buộc cho đăng nhập (chuỗi dài, ngẫu nhiên trên production)
JWT_SECRET=thay-bang-chuoi-bi-mat-it-nhat-32-ky-tu
JWT_EXPIRES_IN=7d

# Tuỳ chọn: override khi chạy docker compose trực tiếp
POSTGRES_PASSWORD=eaai_dev_change_me
POSTGRES_PORT=5432
